# Custom Levels

Level JSON files saved from the Level Editor are stored here.
These files are committed to git and deployed as static assets.

To create a new level:
1. Run `npm run dev` (localhost)
2. Open the Level Editor from the main menu
3. Design your level and click "Save"
4. The file appears here as `Level_Name.json`
5. Commit and push to include in production
